# Product API Final

Run:
1. Copy `.env.example` to `.env` (already included)
2. npm install
3. npm run dev
4. Visit http://localhost:3000/api/docs for Swagger UI

Notes:
- Use the Authorize button in Swagger to set `Bearer <token>`
- To test admin delete, set user role to 'admin' in MongoDB
